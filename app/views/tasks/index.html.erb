<h1>Tasks</h1>

<% @tasks.each do |task| %>
  <p><input type="checkbox" <%= 'checked' if task.completed == true %> >
     <%= link_to task.title, task_path(task.id) %>
  </p>
<% end %>

<%= link_to 'Create new task', new_task_path %>
